// Funcionalidad: Un carrito de compras potenciaría la página al siguiente nivel.

// Interactividad: Posiblemente cambiaría el elige la página a un dropdown 
// y haría permanentemente visible el ver detalles si ya se realizo previamente una compra o este en el storage.

// Escalabilidad: Agregues clases. 

// Integridad: Recomiendo los appends a una etiqueta elegida en vez de al body y que los botones aparezcan en el formulario.

// Legibilidad: Borra el código comentado en desuso.